import { BlurTokens } from '../types/tokens.types.js';

/**
 * Primitive Blur Tokens (Glassmorphism Backdrop Blurs)
 * MarvelVerse Design System - Stage 1.1
 */

export const blurs: BlurTokens = {
  none: 'blur(0px)',
  sm: 'blur(4px)',
  md: 'blur(8px)',
  lg: 'blur(16px)',
  xl: 'blur(24px)',
};
